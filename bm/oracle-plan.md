# Oracle Plan

Pending bm-specific Oracle pass.

The project-wide Oracle pass allowed this conservative skeleton only. Substantial Basic Memory implementation must first include live `bm --help`, selected subcommand help, representative sanitized commands, config/project path evidence, and desired read/write/destructive behavior.
